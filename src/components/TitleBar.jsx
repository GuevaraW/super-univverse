import React from 'react';

import 'scss/components/title-bar.scss';

const TitleBar = () => {
	return (
		<div className="title-bar">
			<h1 className="title-bar-text">SuperUnivverse</h1>
		</div>
	);
};

export default TitleBar;
